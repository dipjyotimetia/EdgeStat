{
  "name": "edgestat-cli",
  "version": "0.1.0",
  "description": "CLI to provision and deploy EdgeStat — analytics at the edge",
  "keywords": ["analytics", "cloudflare", "edge", "cli"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dipjyotimetia/EdgeStat.git",
    "directory": "cli"
  },
  "type": "module",
  "bin": {
    "edgestat": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "@clack/prompts": "^1.2.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^5.7.0",
    "tsx": "^4.21.0"
  }
}
